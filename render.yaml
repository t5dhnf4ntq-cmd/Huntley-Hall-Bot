services:
  - type: web
    name: huntley-helper-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn huntley_helper_bot:app
    envVars:
      - key: GROUPME_BOT_ID
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
